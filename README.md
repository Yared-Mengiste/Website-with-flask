# Flask Portfolio

A simple portfolio website built with Flask. This repository contains a minimal Flask app (`server.py`) serving a single page (`templates/index.html`) and static assets in the `static/` folder.
